###############################################################################
#                          Generated on phpdocker.io                          #
###############################################################################
version: "3.1"
services:

    postgres:
      image: postgres:11.1-alpine
      container_name: laravel-scrapper-postgres
      working_dir: /application
      volumes:
        - .:/application
      environment:
        - POSTGRES_USER=blast06
        - POSTGRES_PASSWORD=blaster07
        - POSTGRES_DB=laravel_scrap
      ports:
        - "8085:5432"

    webserver:
      image: nginx:alpine
      container_name: laravel-scrapper-webserver
      working_dir: /application
      volumes:
          - .:/application
          - ./phpdocker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      ports:
       - "8081:80"

    php-fpm:
      build: phpdocker/php-fpm
      container_name: laravel-scrapper-php-fpm
      working_dir: /application
      volumes:
        - .:/application
        - ./phpdocker/php-fpm/php-ini-overrides.ini:/etc/php/7.3/fpm/conf.d/99-overrides.ini

